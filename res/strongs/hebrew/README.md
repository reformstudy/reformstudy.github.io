# Strong's Hebrew Resource

This folder contains the raw source and the parser for the Strong's Hebrew sample.

Files:
- `hebrew.json` — generated JSON resource
- `process.js` — parser for the raw text (if available)

This folder currently contains the generated JSON file. Add a raw text source and a parser here if you want to regenerate it from unstructured input.
